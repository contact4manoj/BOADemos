@{
}

<h1>ViewModel Demo (Dynamic Demo)</h1>
<p>the Model object is DYNAMIC type object</p>

<ul>
    @foreach(var employee in Model)
    {
        <li> @employee.Name ( @employee.Id ) </li>
    }
</ul>
